# Java 8+ / Streams — Interview Question Bank

1. `map()` vs `flatMap()`?
2. Intermediate vs terminal operations?
3. Why are streams lazy?
4. `reduce()` vs `collect()`?
5. Why can parallel streams reduce performance?
6. Why should stream operations avoid shared mutable state?
7. When should a normal loop be preferred over a stream?
8. What problem does `Optional` solve, and what's a common misuse of it?
