# Streams Questions

### What are lambda expressions? Provide a use case.

Lambda expressions provide a concise way to implement functional interfaces. 
Example: 
java 

List<Integer> list = Arrays.asList(1, 2, 3); 
list.forEach(n -> System.out.println(n));

### Why are streams lazy?

No answer provided yet.

### Why can parallel streams reduce performance?

No answer provided yet.

### Why should stream operations avoid shared mutable state?

No answer provided yet.

### What problem does `Optional` solve, and what's a common misuse of it?

No answer provided yet.

